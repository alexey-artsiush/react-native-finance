import {Text, View} from 'react-native';
import {DataWidget} from '../widgets/DataWidget/ui/DataWidget/DataWidget.tsx';

export const ExpensesScreen = () => {
  return (
    <View>
      <DataWidget />
    </View>
  );
};
